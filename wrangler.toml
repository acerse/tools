name = "cf-tools"
main = "apps/worker/build/worker/shim.mjs"
compatibility_date = "2024-09-02"

[build]
command = "cd apps/worker && cargo install -q worker-build && worker-build --release"

[assets]
directory = "apps/frontend/dist"
